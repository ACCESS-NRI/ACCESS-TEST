name: CD
on:
  push:
    branches:
      - main
      - backport/*.*
    paths:
      - config/**
      - spack.yaml
jobs:
  cd:
    name: CD
    uses: access-nri/build-cd/.github/workflows/cd.yml@277-tar-prerelease-envs-post-install_TEST
    with:
      model: ${{ vars.NAME }}
    permissions:
      contents: write
      pull-requests: write
    secrets: inherit
