name: CD
on:
  push:
    branches:
      - main
      - backport/*.*
    paths:
      - config/**
      - spack.yaml
jobs:
  cd:
    name: CD
    uses: access-nri/build-cd/.github/workflows/cd.yml@237-skip-check-spack-yaml-check-on-draft_TEST
    with:
      model: ${{ vars.NAME }}
    permissions:
      contents: write
      pull-requests: write
    secrets: inherit
